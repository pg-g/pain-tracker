var app = angular.module('migraineApp');

// var firebaseObj = new Firebase('https://migraine.firebaseio.com/stats');

app.controller('logControl', function($scope, $location){
	$scope.logTest = 'logged!';

	$scope.logData = function(migraine){

	};
})